import React from 'react'

const WarningMessage = (props) => {

    const message = 'You are seeing the default warning message'

    return (
        <div>
            {message}
        </div>
    )

}

export default WarningMessage